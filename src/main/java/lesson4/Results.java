package lesson4;

public enum Results {
	PASSED,
	FAILED,
	BLOCKED;

	Results results = Results.PASSED;
	if (result == Result.PASSED) {
		
	}
	 else if (result == Result.FAILED) {
		
	 }
	 else if (result == Result.BLOCKED) {
		 System.out.println("It’s impossible to run this test case");
		}
	}
}


